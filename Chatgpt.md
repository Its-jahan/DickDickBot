# گزارش اجرای استپ‌های ۴ و ۵

تاریخ شروع: ۱۴۰۵/۰۶/۲۱ (2026-09-12)

این فایل مرجع تصمیم‌ها، تغییرات، نتیجه‌ها و خروجی‌های این مرحله است و هم‌زمان با پیشرفت کار به‌روزرسانی می‌شود.

## درخواست و محدوده

- ادامهٔ پروژه بعد از استپ ۳.
- استپ ۴: خرید آیتم و بستهٔ سانتی‌متر با Telegram Stars.
- استپ ۵: انتخاب لحن فعلی بزرگسال (+18) یا لحن مودب.
- رفتارهای اصلی اقتصادی و بازی که در `CLAUDE.md` ثبت شده‌اند باید حفظ شوند.
- تغییر موجود و از قبل stage‌شدهٔ `deploy/nginx-admin.inddex.app.conf` متعلق به کاربر است و نباید بازنویسی شود.

## تصمیم‌های اولیه

1. تنظیم لحن در سطح هر گروه نگهداری می‌شود؛ پیش‌فرض «بزرگسال» است تا رفتار فعلی گروه‌های موجود عوض نشود.
2. تغییر لحن فقط باید توسط ادمین گروه انجام شود.
3. پرداخت Stars با ارز `XTR` و بدون `provider_token` انجام می‌شود و اعتبار فقط بعد از رویداد `successful_payment` به کاربر داده می‌شود.
4. هر پرداخت یک شناسهٔ یکتای payload و رکورد پایدار دیتابیس خواهد داشت تا تحویل دوباره یا آپدیت تکراری Telegram نتواند آیتم/سانت را دوبار اضافه کند.
5. بستهٔ سانتی‌متر یک منبع پول (source) است، نه انتقال یا خزانه؛ بنابراین مقدار و منبع آن باید در ledger ثبت شود.
6. خرید آیتم با Stars به موجودی آیتم اضافه می‌کند و از `users.size` یا خزانهٔ گروه چیزی کم نمی‌کند.
7. قیمت اولیهٔ بسته‌ها برای شروع: ۵۰ سانت = ۲۵ Stars، ۱۵۰ سانت = ۶۰ Stars، ۴۰۰ سانت = ۱۴۰ Stars. قیمت آیتم‌ها بین ۴ تا ۲۰ Stars است و همهٔ این اعداد از یک کاتالوگ واحد در `bot.py` خوانده می‌شوند تا بات و اپ اختلاف قیمت پیدا نکنند.
8. خرید Stars سهمیه و تورم فروشگاه سانتی را مصرف نمی‌کند؛ چون ممکن است پرداخت چند ثانیه بعد تسویه شود و کالای پرداخت‌شده نباید به‌خاطر تمام‌شدن سهمیه رد شود.
9. پاسخ `openInvoice` در مرورگر فقط برای نمایش وضعیت است و هیچ اعتباری ایجاد نمی‌کند؛ تنها `successful_payment` رسمی Telegram مجاز به تحویل خرید است.
10. متن‌های ذخیره‌شده، شناسهٔ آیتم‌ها و قوانین بازی تغییر نمی‌کنند؛ لحن در آخرین مرز نمایش رندر می‌شود تا تغییر واژگان نتواند منطق بازی را خراب کند.
11. خرید موفق هم در گروه اعلام می‌شود و هم در جدول `events` ثبت می‌شود تا تب رویدادها منبع مستقل خود را حفظ کند.

## بررسی‌های انجام‌شده

- `CLAUDE.md` و `README.md` کامل خوانده شدند.
- در بررسی اولیه، شاخهٔ محلی قدیمی بود و فرانت‌اند را نداشت. مقایسهٔ مستقیم با GitHub نشان داد HEAD محلی عقب است؛ پس آخرین `main` دریافت شد.
- مستندات رسمی shadcn/ui دربارهٔ نصب، تم، کامپوننت‌ها، typography و RTL بررسی شد.
- فولدر با حفظ تغییر stage‌شدهٔ کاربر تا commit `2ce79c60c264b62ebdcf2b7fbb22d8f43e4f937d` fast-forward شد؛ این SHA در زمان بررسی دقیقاً برابر `main` روی GitHub بود.
- نسخهٔ همگام‌شده شامل خروجی استپ‌های ۱ تا ۳ است: Mini App با React/Vite/shadcn، تم روشن/تیره، قیمت و چارت واقعی کریپتو، و اجرای انتقال/دزدی/اهدای سایز از اپ.
- پس از sync، نسخهٔ جدید و کامل `CLAUDE.md` (۱۸۷۳ خط) دوباره و کامل خوانده شد.
- در پایان کار نیز `HEAD` محلی، `origin/main` و `main` مستقیم GitHub هر سه دوباره بررسی شدند و هر سه روی `2ce79c60c264b62ebdcf2b7fbb22d8f43e4f937d` بودند. تغییرات استپ ۴ و ۵ هنوز محلی و commit‌نشده‌اند.

## خروجی استپ ۴ — Telegram Stars

- جدول پایدار `star_orders` و ایندکس‌های لازم در `db.init_db` اضافه شد.
- فاکتور با `XTR`، یک قیمت، payload یکتای UUID و بدون provider token ساخته می‌شود.
- خریدار، مبلغ و ارز در pre-checkout دوباره بررسی می‌شوند؛ سفارش در همان لحظه اتمیک از `created` به `checkout` می‌رود.
- تحویل آیتم یا سانت و ثبت charge id در یک تراکنش انجام می‌شود. ارسال دوبارهٔ همان update نتیجهٔ `duplicate` می‌دهد و موجودی را دوباره زیاد نمی‌کند.
- سه بستهٔ سانتی و تمام ۱۳ آیتم از `/stars` و فروشگاه Mini App قابل خرید شدند.
- Mini App با `openInvoice` فاکتور را باز می‌کند و پس از پرداخت، وضعیت سفارش را از سرور می‌خواند؛ تحویل وابسته به ادعای مرورگر نیست.
- `/paysupport` و متغیر اختیاری `PAYMENT_SUPPORT_CONTACT` برای مسیر پشتیبانی اضافه شد.
- خرید موفق در گروه اعلام و در feed ثبت می‌شود.

## خروجی استپ ۵ — لحن گروه

- ستون `chats.tone_mode` اضافه شد؛ مقدار مجاز `adult` یا `polite` و پیش‌فرض `adult` است.
- فرمان `/tone` و پنل تنظیمات Mini App اضافه شدند.
- فقط creator/administrator گروه بعد از بررسی `getChatMember` می‌تواند لحن را تغییر دهد.
- پیام‌های عادی، ادیت‌ها، callback alertها، پیام‌های inline قابل‌تشخیص و اعلان‌های ارسال‌شده از وب از رندر لحن مشترک عبور می‌کنند.
- پاسخ‌های Mini App برای گروه محترمانه در سمت نمایش تبدیل می‌شوند؛ فیلدهای جداگانهٔ نام کاربر و نام گروه دست‌نخورده می‌مانند.
- cache لحن بات ۶۰ ثانیه است؛ تغییر داخل بات فوری cache را تازه می‌کند و تغییر از سرویس وب حداکثر بعد از این TTL در process بات دیده می‌شود.

## فایل‌های تغییرکرده

- `python_bot/db.py`: schema، تنظیم لحن و چرخهٔ امن سفارش Stars.
- `python_bot/bot.py`: کاتالوگ، فاکتور، pre-checkout، تحویل، فرمان‌های `/stars`، `/tone` و `/paysupport` و رندر لحن Telegram.
- `python_bot/webapp.py`: ساخت Invoice Link، وضعیت سفارش، تغییر لحن با احراز ادمین و دادهٔ فروشگاه.
- `python_bot/web/src/screens/Shop.tsx`: خرید Stars برای بسته‌ها و آیتم‌ها.
- `python_bot/web/src/components/ToneSettings.tsx`: تنظیمات لحن گروه.
- `python_bot/web/src/lib/tone.ts` و `App.tsx`: رندر لحن در Mini App.
- `python_bot/web/src/screens/Feed.tsx`: نمایش نوع رویداد Stars.
- `python_bot/web/dist/*`: خروجی production بازسازی‌شده.
- `python_bot/requirements.txt`: حداقل نسخهٔ سازگار `python-telegram-bot` به `21.11` ارتقا یافت.
- `python_bot/test_stars_tone.py`: تست رگرسیون پرداخت، idempotency، دسترسی ادمین و لحن.
- `README.md` و `CLAUDE.md`: رفتار و invariantهای جدید مستند شدند.

## تست و نتیجه

- `python -m py_compile`: موفق برای `bot.py`، `db.py`، `webapp.py` و تست جدید.
- `tsc --noEmit`: موفق، بدون خطای TypeScript.
- `npm run build`: موفق؛ ۲۲۸۶ ماژول تبدیل و خروجی production جدید در `web/dist` ساخته شد.
- تست واقعی روی PostgreSQL موقت و خالی: ۴ تست از ۴ تست موفق.
- موارد پوشش‌داده‌شده: ارز/مبلغ/خریدار اشتباه، تحویل یک‌بارهٔ بسته، جلوگیری از charge تکراری، خرید آیتم، ledger با منبع `telegram_stars`، پیش‌فرض/تغییر لحن، invoice endpoint اپ و ممنوعیت تغییر لحن برای غیرادمین.
- `git diff --check`: موفق و بدون خطای whitespace.
- پرداخت واقعی با حساب Telegram انجام نشد؛ این کار خرج واقعی و اثر بیرونی دارد. مسیر فاکتور و تحویل با اشیای واقعی کتابخانه و دیتابیس PostgreSQL موقت تست شد.
- فایل stage‌شدهٔ قبلی کاربر، `deploy/nginx-admin.inddex.app.conf`، دست‌نخورده باقی ماند.

## وضعیت

- استپ‌های ۴ و ۵ پیاده‌سازی و تست شدند.
- کاربر دستور مستقیم «پوش کن رو مین» داد؛ انتشار تغییرات استپ ۴ و ۵ روی `main` مجاز شد.

## انتشار روی main — 2026-09-12

- آخرین `origin/main` دوباره دریافت شد؛ قبل از کامیت، شاخهٔ محلی و GitHub هیچ اختلافی نداشتند.
- بررسی `git diff --check` موفق بود و دسترسی حساب GitHub تأیید شد.
- کامیت `457cb47bb5026ec69bb273efe7b1c2cdd3bf5bb6` با عنوان `Add Telegram Stars purchases and per-group tone settings` ساخته و با موفقیت روی `origin/main` پوش شد.
- کامیت انتشار شامل ۱۵ فایل کد، خروجی build، تست و مستندات همین مرحله است. تغییر stage‌شدهٔ قبلی `deploy/nginx-admin.inddex.app.conf` خارج از کامیت و دست‌نخورده باقی ماند.
- [بررسی تطابق build با سورس](https://github.com/Its-jahan/DickDickBot/actions/runs/34662471327): با نتیجهٔ `success` تمام شد.
- [انتشار خودکار روی سرور](https://github.com/Its-jahan/DickDickBot/actions/runs/34662471265): با نتیجهٔ `success` تمام شد.
- نتیجهٔ نهایی: کد استپ ۴ و ۵ روی `main` منتشر شد و هر دو workflow موفق شدند. پرداخت واقعی Telegram همچنان انجام نشده است.
- این نتیجه در یک کامیت مستنداتی با `[skip ci]` ذخیره می‌شود تا فقط برای ثبت گزارش، انتشار دوبارهٔ سرویس‌ها اجرا نشود.

## پرسش دربارهٔ مقصد Stars — 2026-09-12

- Stars پرداخت‌شده وارد دیتابیس پروژه، خزانهٔ سانتی گروه یا کیف پول داخل بازی نمی‌شوند؛ Telegram آن‌ها را در موجودی Stars خودِ بات نگه می‌دارد.
- کد ما فقط سفارش داخلی، مبلغ، شناسهٔ charge و تحویل آیتم را در `star_orders` ثبت می‌کند و هیچ مسیر تبدیل Stars به سانت خزانه، انتقال به حساب شخصی یا برداشت خودکار ندارد.
- موجودی و تراکنش‌های بات از بخش Balance/Monetization حساب بات در Telegram قابل پیگیری است؛ Bot API رسمی هم `getMyStarBalance` و `getStarTransactions` را برای صاحب بات ارائه می‌کند.
- برای برداشت، Telegram موجودی قابل‌برداشت را پس از دورهٔ انتظار خودش مشخص می‌کند و مسیر برداشت به صفحهٔ Fragment و کیف پول TON می‌رسد؛ این مرحله در کد پروژه خودکار نشده است.
- برای refund باید از `refundStarPayment` با user id و `telegram_payment_charge_id` استفاده شود؛ فعلاً مسیر پشتیبانی `/paysupport` وجود دارد و refund خودکار/دستور مدیریتی در این مرحله اضافه نشده است.
- مرجع بررسی: [Telegram Stars](https://core.telegram.org/api/stars)، [Bot API](https://core.telegram.org/bots/api) و [شرایط Stars](https://telegram.org/tos/stars).

## نحوهٔ رسیدن درآمد Stars به مالک بات — 2026-09-12

- Telegram درآمد را به حساب بانکی یا موجودی شخصی Stars مالک واریز نمی‌کند؛ درآمد ابتدا در Balance/Monetization همان بات و تحت کنترل اکانتی قرار می‌گیرد که در BotFather مالک بات است.
- اگر بات در BotFather متعلق به اکانت شخص دیگری باشد، کنترل موجودی و برداشت نیز در اختیار همان مالک است؛ داشتن سورس‌کد یا bot token به‌تنهایی مالکیت درآمد را منتقل نمی‌کند.
- Stars دریافتی ابتدا در Total Balance دیده می‌شوند و طبق سیاست فعلی Telegram معمولاً ۲۱ روز بعد وارد Available Balance می‌شوند.
- مالک بات می‌تواند موجودی قابل‌استفاده را برای Telegram Ads خرج کند یا از بخش برداشت، لینک Fragment بگیرد و پاداش را به کیف پول TON منتقل کند. برداشت مستقیم ریالی/دلاری یا واریز خودکار به کارت بانکی وجود ندارد.
- برداشت به فعال‌بودن 2FA، رسیدن موجودی به حداقل متغیر Telegram و بازبودن برداشت برای حساب/منطقه وابسته است. مبلغ حداقل در کد پروژه ثابت نشده چون Telegram آن را از تنظیمات جاری خودش اعلام می‌کند.

## محل دقیق نمایش موجودی بات — 2026-09-12

- تصاویر کاربر نشان می‌دهند مسیر BotFather → Monetization → Telegram Stars باز شده است. پنجرهٔ `Learn More` این قسمت صفحهٔ معرفی قابلیت است و صفحهٔ Balance بات نیست.
- مسیر رسمی نمایش موجودی: چت خود بات `@dickchallengerbot` → بازکردن Profile بات → `Edit` → `Balance`.
- بررسی فقط‌خواندنی با API رسمی `getMyStarBalance` و `getStarTransactions` تأیید کرد این بات اکنون ۸ Stars و یک تراکنش دارد؛ بنابراین اتصال پرداخت کار کرده و موجودی واقعاً در حساب بات ثبت شده است.
- اگر گزینهٔ `Balance` در نسخهٔ فعلی Telegram دیده نشود، باید همان پروفایل بات را در آخرین نسخهٔ رسمی Telegram موبایل باز کرد. مقدار از API قابل مشاهده است و وابسته به نمایش BotFather نیست.
- مشاهدهٔ امنیتی: bot token فعلی در `bot.py` به‌صورت مستقیم نوشته و در repository منتشر شده است. مقدار token در این گزارش ثبت نشد؛ برای امن‌سازی باید token از BotFather تعویض و سپس فقط از secret/environment خوانده شود. این تغییر نیازمند هماهنگی با تنظیمات production است و در پاسخ حاضر انجام نشد.

## تنظیم پیشنهادی تبلیغ Telegram Ads با بودجهٔ ۵ دلار — 2026-09-12

- نرخ رسمی حساب تبلیغاتیِ Stars برای مالک بات، هر Star برابر ۰٫۰۲ دلار اعتبار تبلیغاتی است؛ بنابراین سقف این تست ۲۵۰ Stars است.
- نوار بالای تصویر `Budget: 0` را نشان می‌دهد. موجودی ۸ Stars فعلی بات فقط معادل ۰٫۱۶ دلار اعتبار تبلیغاتی است و هنوز برای این تست کافی نیست؛ برای رسیدن به بودجهٔ ۵ دلار، ۲۴۲ Stars دیگر لازم است. Stars درآمدی ممکن است تا ۲۱ روز در حالت pending بماند.
- به‌دلیل کوچک بودن بودجه، تصمیم شد فقط یک تبلیغ و یک گروه هدف اجرا شود و بودجه بین چند نسخه تقسیم نشود.
- تنظیم پیشنهادی فرم:
  - Target: `Channels`
  - Ad title (فقط برای مدیریت داخلی): `FA | Group Game | $5 Test 01`
  - Ad text: `بازی رقابتی برای گروه‌های تلگرام 🎮 با دوستات رقابت کن، رشد کن، آیتم بگیر و صدر جدول گروه رو فتح کن. ربات رو به گروه اضافه کن و شروع کن!`
  - طول متن: ۱۳۵ کاراکتر و کمتر از سقف رسمی ۱۶۰ کاراکتر.
  - URL: `https://t.me/dickchallengerbot`
  - Show picture: برای تست اول خاموش؛ تصویر نامناسب یا دوپهلو ریسک ردشدن را بالا می‌برد.
  - CPM in Stars: شروع با `50`؛ اگر خود فرم حداقل دیگری نشان داد، حداقل جاری همان فرم ملاک است. اگر پس از تأیید نمایش نگرفت، CPM در گام‌های ۱۰٪ تا ۲۰٪ بالا برود.
  - Initial budget in Stars: `250`
  - Daily budget in Stars: `125` برای یک تست حداکثر دو روزه.
  - Daily views limit per user: `1`
  - Initial status: تا زمان آماده‌سازی پروفایل `On Hold` و پس از آن `Active`.
  - Target channel languages: فقط `Persian`.
  - Target topics: فقط دو دستهٔ نزدیک به `Games/Gaming` و `Humor & Entertainment`؛ نام دقیق گزینه ممکن است در رابط فرق کند.
  - Target specific channels: خالی؛ با این بودجه بدون فهرست کانال‌های بررسی‌شده، محدودکردن به چند کانال ریسک صفرشدن نمایش را دارد.
  - Exclude topics و Exclude specific channels: خالی؛ چون زبان و دو موضوع مثبت از قبل دامنه را محدود می‌کنند.
- معیار موفقیت این تست «Start شدن بات بعد از مشاهدهٔ تبلیغ» است، نه فقط impression. آمار رسمی Ads شروع بات پس از مشاهده را گزارش می‌کند.
- ریسک مهم تأیید: سیاست رسمی Telegram فحش، محتوای جنسی یا دوپهلو را هم در تبلیغ و هم در مقصد ممنوع می‌کند و نام/برند مقصد نیز بررسی می‌شود. بنابراین username فعلی `dickchallengerbot`، نام DickDick و حالت adult پیش‌فرض می‌توانند باعث ردشدن شوند؛ متن تمیز این ریسک را حذف نمی‌کند. پیش از خرج بودجه باید نام نمایشی، عکس، About/Description و تجربهٔ شروع ربات کاملاً محترمانه باشند و برای شانس تأیید بالا، یک username عمومی خنثی در نظر گرفته شود.
- مرجع‌ها: [Telegram Ad Policies and Guidelines](https://ads.telegram.org/guidelines)، [Telegram Ad Platform Explained](https://ads.telegram.org/getting-started)، [Telegram Bot Platform Developer Terms](https://telegram.org/tos/bot-developers) و [Telegram Stars API](https://core.telegram.org/api/stars).

## اصلاح بودجه برای حساب تبلیغاتی Gram — 2026-09-12

- تصویر جدید مربوط به حساب شخصی `Jahan` با واحد Gram است، نه حساب تبلیغاتی Stars بات؛ بنابراین اعداد ۲۵۰ و ۱۲۵ Stars برای این فرم استفاده نمی‌شوند.
- Gram نام فعلی رمزارز شبکهٔ TON است. قیمت مشاهده‌شده در زمان بررسی حدود ۱٫۳۵ دلار برای هر Gram بود؛ پس بودجهٔ ۵ دلار تقریباً `3.70 Gram` است. این تبدیل با قیمت بازار تغییر می‌کند.
- تنظیم بودجهٔ این فرم:
  - `Initial budget in Gram`: مقدار `3.70`.
  - در صورت فعال‌کردن daily limit برای تست دو روزه: `1.85 Gram` در روز.
  - `Daily views limit per user`: مقدار `1`.
- حداقل CPM منتشرشدهٔ Telegram برابر `0.10 Gram` برای هزار نمایش است.
- در تصویر گزینهٔ `Show bot picture` روشن است و خود فرم `+30%` نشان می‌دهد. با واردکردن CPM پایهٔ `0.10`، هزینهٔ مؤثر این فرمت `0.13 Gram` برای هزار نمایش می‌شود. در این حالت بودجهٔ ۳٫۷۰ Gram از نظر محاسباتی حداکثر حدود ۲۸٬۴۶۰ نمایش می‌دهد؛ تحویل واقعی به رقابت مزایده وابسته است.
- اگر `Show bot picture` خاموش شود، CPM پایه همان `0.10 Gram` می‌ماند و سقف تئوری بودجه حدود ۳۷٬۰۰۰ نمایش است. با توجه به کوچک بودن بودجه، تصمیم اقتصادی‌تر خاموش‌کردن عکس است؛ روشن‌بودن آن فقط در صورت ارزش بصری و شانس کلیک بهتر توجیه دارد.
- در نوار بالا `Budget: 0.00` دیده می‌شود؛ بنابراین قبل از اجرای تبلیغ باید موجودی حساب Gram شارژ شود. `Initial budget` سقف مصرف کمپین است و لزوماً با حداقل مبلغ اولین شارژ حساب یکی نیست. اگر صفحهٔ Add Funds حداقلی بیشتر از ۳٫۷۰ Gram الزام کند، بودجهٔ نقدی ۵ دلار برای شروع این حساب کافی نیست و نباید مبلغ بیشتری بدون تصمیم جداگانه واریز شود.
- سایر تنظیمات هدف‌گیری بدون تغییر: `Channels`، زبان `Persian`، موضوع‌های نزدیک به `Games/Gaming` و `Humor & Entertainment`، فرکانس روزانه ۱ و خالی‌گذاشتن specific/excludeها در تست اول.
- منابع بررسی: [راهنمای رسمی Telegram Ads](https://ads.telegram.org/getting-started)، [شرایط Telegram Ads](https://ads.telegram.org/tos)، [توضیح رسمی درآمد Gram](https://core.telegram.org/api/revenue) و [قیمت لحظه‌ای GRAM در CoinMarketCap](https://coinmarketcap.com/currencies/gram/).
